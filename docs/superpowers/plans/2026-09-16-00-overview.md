# Code Review Remediation — Plan Overview (2026-09-16)

**Spec:** `docs/superpowers/specs/2026-09-16-code-review-findings.md`

Five plans, one per subsystem. Each produces working, tested software on its
own and is merged as its own PR. Execute in the order below; the dependency
notes say where two plans touch the same file.

| # | Plan | Spec items | Depends on | Touches shared files |
|---|------|-----------|------------|----------------------|
| A | `2026-09-16-a-session-lifecycle.md` | A1–A7 | — | `src/main.ts`, `src/lib/jobs/potoken.ts`, `src/lib/helpers/metrics.ts`, `src/routes/readiness.ts` |
| B | `2026-09-16-b-video-proxy-fetch-client.md` | B1–B7 | — | `src/lib/helpers/getFetchClient.ts`, `src/routes/videoPlaybackProxy.ts`, `src/lib/helpers/metrics.ts` |
| C | `2026-09-16-c-security-routes.md` | C1–C11 | — | all `src/routes/*`, `src/main.ts` (app setup), `src/lib/helpers/config.ts`, `src/lib/helpers/metrics.ts`, `src/lib/helpers/log.ts` |
| D | `2026-09-16-d-player-handling.md` | D1–D5 | A (sharedState) | `src/main.ts` (sharedState), `src/lib/helpers/youtubePlayerHandling.ts`, routes |
| E | `2026-09-16-e-tests-ci-ops.md` | E1–E10 | B, C (route tests) | `src/tests/*`, `.github/workflows/*`, `Dockerfile`, `docker-compose.yaml`, `README.md`, `update.sh` |

## Recommended order

1. **A** first. It fixes the only CRITICAL finding (dead-minter outage) and
   moves session state out of `main.ts`, which D builds on.
2. **B** and **C** can run in parallel on separate branches. Both edit
   `metrics.ts` (additive counters only) and C edits the route files that B's
   video-proxy tests exercise; rebase whichever merges second.
3. **D** after A has merged (it reads `sharedState.sessionGeneration`).
4. **E** last. It assumes the route tests from B and C exist and finishes the
   CI/ops hardening around them.

## Ownership of overlapping files

Two plans touch the same file in these cases. The owner's version wins; the
other plan's task says what to do when the owner has already merged.

| File | Owner | Other plan | Rule |
|------|-------|-----------|------|
| `src/tests/shutdown_test.ts` | A (Task 1) | E (Task 7) | E Task 7 is skipped once A has merged. |
| `src/tests/verifyRequest_test.ts`, `src/tests/helpers/check.ts` | C (Task 4) | E (Task 5) | E Task 5 only adds missing cases to C's file and reuses `makeCheck`. |
| `src/tests/dynamicImportValidation_test.ts` | C (Task 1, adds bypass cases) | E (Task 3, migrates to `withEnv`) | Run C first; E migrates whatever exists. |
| `src/lib/helpers/youtubePlayerHandling.ts` cache-write blocks | D (Task 1/4, `playerCache.ts`) | A (Task 7, tracked writes) | A Task 7 wraps `writePlayerCache(...)` with `trackPendingWrite` if D landed first; otherwise wraps the IIFEs and D moves the tracking into `playerCache.ts`. |
| `src/lib/helpers/metrics.ts` | — | A (Task 2), B, C (Task 10) | Additive counters only; no conflicts expected beyond rebase. |
| `src/main.ts` | A (Task 5, extracts session state) | C (Task 2 `onError`, Task 8 rate limit), D (Task 5 `sessionGeneration`) | Run A first. C and D anchor on code content, not line numbers; if A moved a block into `src/lib/session/`, apply the edit there. |
| Test helpers | A `helpers/fakeWorker.ts`, C `helpers/testConfig.ts` + `helpers/check.ts`, E `helpers/env.ts` | — | Distinct files. E Task 1 must not duplicate `makeTestConfig`; plan B's inline `SERVER_SECRET_KEY` try/finally blocks may be replaced by `withEnv` after E merges. |

## Merge rules

- Every plan ends with the full gate: `deno task format && deno task check &&
  deno task lint && SERVER_SECRET_KEY=aaaaaaaaaaaaaaaa deno task test`.
- `main.ts` is edited by A (session extraction), C (`onError`, rate-limit
  middleware) and D (`sessionGeneration`). Keep each change additive and
  rebase before opening the PR.
- Nothing in these plans changes the Invidious contract except where the spec
  says so explicitly (currently: none). If a task discovers it must, stop and
  update `../invidious/` in the same PR.

## Status

- [ ] A merged
- [ ] B merged
- [ ] C merged
- [x] D merged
- [ ] E merged
